<div class="container">
  <div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1>Les dragons</h1>
      <ul class="list-group mb-3">
        <% @dragons.each do |dragon| %>
          <li class="list-group-item">
            <%= link_to dragon.name, dragons_path(dragons) %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>

<%# Afficher la photo de chaque dragon - photo Active Storage
Afficher le nom de chacun des dragons - name
Afficher la nature des dragons - nature
Afficher le prix par jour - price_per_day %>
